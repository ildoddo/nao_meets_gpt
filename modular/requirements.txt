# NAO Conversational System - Requirements

# Core
numpy>=1.24.0
requests>=2.31.0

# Speech Recognition
SpeechRecognition>=3.10.0

# Whisper (opzionale - solo se usi TRANSCRIPTION_METHOD="whisper")
# Decommenta la riga sotto se vuoi usare Whisper offline:
# faster-whisper>=0.10.0

# LLM
litellm>=1.17.0
tiktoken>=0.5.0

# Environment variables (opzionale ma consigliato)
# python-dotenv>=1.0.0

# Optional (for noise reduction)
# noisereduce>=3.0.0
